# Projeto RL — Otimização de Promoções (V21 PDV-native)
# Python 3.11 recomendado. Instalar: pip install -r requirements.txt
gymnasium>=0.29
torch>=2.0
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1   # leitura do inventário .xlsx (opcional — só processar_inventario.py)
